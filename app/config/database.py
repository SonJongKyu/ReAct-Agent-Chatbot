"""
(260120) 챗봇 유저(user_id)에 따른 대화 기록 DB (PostgreSQL 전용) - 현재는 사용 X 
"""
# import os
# from dotenv import load_dotenv

# load_dotenv()

# DATABASE_URL = os.getenv("DATABASE_URL")

# def get_database_url():
#     if not DATABASE_URL:
#         raise ValueError("DATABASE_URL이 .env 파일에 설정되지 않았습니다!")
#     return DATABASE_URL